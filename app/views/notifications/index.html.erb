<div class="container">
  <h2 class="text-center">通知</h2>
      <%= link_to notifications_delete_path, method: :delete do %>
        <h3 class="btn btn-danger text-white ml-1"> 全削除</h3>
      <% end %>
      <hr>
  <% if @notifications.exists? %>
      <div class="users-index">
          <%= render @notifications %>
      </div>
  <% else %>
      <p>通知はありません</p>
  <% end %>
</div>